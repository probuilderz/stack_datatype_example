name: Building

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2-beta
    #- name: configure
    #  run: ./configure
    - name: Build
      run: make
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
